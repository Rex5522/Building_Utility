function gpsv2(value,ofsetx,ofsetz,directional)
	if type(value)=="table" then
		local x,y,z = matrix.position(value)
		value = matrix.translation(x+ofsetx,y,zofsetz)
		return value
		
		
		
		
		
			
		
		
		
	end
end
